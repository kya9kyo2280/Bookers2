

<div class="container">
      <div class="row">

          <div class="col-lg-3" >
            <%= render 'books/list', book: @newbook ,user: @user %>
          </div>
          <div class="col-lg-9">
<h2>Users</h2>
<table class="table table-striped">
  <thead>
    <tr>
      <th>image</th>
      <th> name</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><div class="form-group has-icon">
   <div class="user-profile-image">
    <%= attachment_image_tag user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "50x50" %>
   </div>
</div></td>
  </div>
      <td><%= user.name %></td>
      <td><%= link_to 'Show', user_path(user.id) %></td>

    </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>