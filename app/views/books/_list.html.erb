

<ul class="nav nav-sidebar">
<table class="table table-striped">

	<div class="user info">
<h2>User info</h2>

<div class="user-profile-image">
  <%= attachment_image_tag @user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "100x100" %>
</div>

  <p class="orange b">Name</p>
　<p><%= @user.name %></p>

  <p class="orange b">Intoroduction</p>
  <p><%= @user.introduction %></p>



    <% if @user.id == current_user.id %>
        <p><%= link_to (edit_user_path(@user)) do %></p>
        <span class="glyphicon glyphicon-wrench btn btn-default"  aria-hidden="true"> </span>
        <% end %>
    <% end %>

    </div>

     <div class="new">

　　<h2>New Book</h2>

　　<%= form_for(@book) do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Body</h4>
    <%= f.text_area :body %><br>

　　<%= f.submit 'Create Book' ,class: " btn btn-info btn-lg btn-block" %>
　　<% end %>

   </div>

</ul>
</table>

